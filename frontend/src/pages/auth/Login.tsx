import PagePlaceholder from '../../components/PagePlaceholder'

export default function Login() {
  return (
    <PagePlaceholder
      title="Login"
      todo="build the login form (email/password) with client-side validation and role-based redirect."
    />
  )
}
