import PagePlaceholder from '../../components/PagePlaceholder'

export default function Register() {
  return (
    <PagePlaceholder
      title="Register"
      todo="build the registration form (email/password) with client-side validation."
    />
  )
}
