import PagePlaceholder from '../../components/PagePlaceholder'

export default function QueueStatus() {
  return (
    <PagePlaceholder
      title="Queue Status"
      todo="show current position, estimated wait time, and status (waiting / almost ready / served)."
    />
  )
}
