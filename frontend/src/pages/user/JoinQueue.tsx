import PagePlaceholder from '../../components/PagePlaceholder'

export default function JoinQueue() {
  return (
    <PagePlaceholder
      title="Join Queue"
      todo="let the user pick a service, view the estimated wait time, and join/leave the queue. Reuse services data from src/context/ServicesContext."
    />
  )
}
